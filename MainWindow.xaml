<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<Window
    x:Class="ModernLife.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    xmlns:local="using:ModernLife"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <SplitView x:Name="splitView" IsPaneOpen="True" OpenPaneLength="200" DisplayMode="Inline" PanePlacement="Right"
               PaneBackground="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
        <SplitView.Pane>
            <StackPanel Orientation="Vertical" >
                <TextBlock Text="MODERN LIFE" x:Name="PaneHeader" HorizontalAlignment="Center" Margin="0,12,0,12" Style="{StaticResource BaseTextBlockStyle}"/>
                <AppBarToggleButton BorderBrush="AntiqueWhite" HorizontalAlignment="Center" Icon="Pause" x:Name="GoButton"  Label="Pause" Click="GoButton_Click" />
                <AppBarToggleButton BorderBrush="AntiqueWhite" HorizontalAlignment="Center" Icon="Shuffle" x:Name="RestartButton"  Label="Restart" Click="RestartButton_Click" />
                <Grid Margin="6" HorizontalAlignment="Stretch" Height="400">
                    <canvas:CanvasControl x:Name="theCanvasDebug" Draw="theCanvasDebug_Draw" />
                </Grid>
            </StackPanel>
        </SplitView.Pane>
        <Grid Margin="6" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" BorderThickness="1" BorderBrush="Red">
            <canvas:CanvasControl x:Name="theCanvas" Draw="CanvasControl_Draw" ClearColor="WhiteSmoke" />
        </Grid>
    </SplitView>
</Window>
